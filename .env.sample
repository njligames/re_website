
# PostgreSQL connection string (required)
export DATABASE_URL=
export SMTP_HOST=
export SMTP_PORT=
export SMTP_USER=
export SMTP_PASSWORD=
export NOTIFY_EMAIL=   # omit if same as SMTP_USER
export SITE_DOMAIN=
# Google Maps JavaScript API key — enables Google Maps on property pages (optional; falls back to OpenStreetMap)
export GOOGLE_MAPS_API_KEY=

